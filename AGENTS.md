# Special rule for calling custom subagent type

> Follow this in the workspace!

When trying to call custom subagent type (for example poteto-agent)

Use `subagent_type: generalPurpose`, prompt: <path to skill to inject> as a replacement of missing custom subagent type, or the sub agent will return error.